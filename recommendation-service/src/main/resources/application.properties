spring.application.name=recommendation-service
server.port=8084

spring.datasource.url=jdbc:postgresql://localhost:5432/pcbuilder
spring.datasource.username=postgres
spring.datasource.password=admin

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.table=flyway_schema_history_recommendation
spring.flyway.baselineOnMigrate=true

eureka.client.enabled=${EUREKA_CLIENT_ENABLED:false}
eureka.client.service-url.defaultZone=${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}

# LLM (Ollama / DeepSeek). Loaded only from this file and OS environment — not from PostgreSQL.
# Host bootRun: 127.0.0.1. Docker: set LLM_URL=http://host.docker.internal:11434/v1/chat/completions
llm.enabled=${LLM_ENABLED:true}
llm.model=${LLM_MODEL:deepseek-r1:14b}
llm.url=${LLM_URL:http://127.0.0.1:11434/v1/chat/completions}

# Part Service URL via Eureka
part-service.url=http://localhost:8081

# Logging tweaks: reduce Hibernate noise and surface recommendation-service logs
logging.level.org.hibernate=ERROR
logging.level.org.springframework.orm.jpa=ERROR
logging.level.org.springframework=ERROR
logging.level.org.example.recommendationservice=DEBUG
logging.level.org.hibernate.SQL=ERROR
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=ERROR
logging.pattern.console=%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n
