eureka.client.enabled=false
# Required by @Value("${auth.validate-url}") — main file default is not always visible during sliced test resolution.
auth.validate-url=http://127.0.0.1:8082/auth/validate
security.jwt.secret=test-jwt-secret-at-least-thirty-two-characters-long-ok
